services:

  # =======================================================
  # LARAVEL APP
  # =======================================================

  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: e-arsip-app

    restart: unless-stopped

    environment:
      APP_NAME: ${APP_NAME}
      APP_ENV: ${APP_ENV}
      APP_KEY: ${APP_KEY}
      APP_DEBUG: ${APP_DEBUG}
      APP_URL: ${APP_URL}

      APP_LOCALE: ${APP_LOCALE}
      APP_FALLBACK_LOCALE: ${APP_FALLBACK_LOCALE}
      APP_FAKER_LOCALE: ${APP_FAKER_LOCALE}

      APP_MAINTENANCE_DRIVER: ${APP_MAINTENANCE_DRIVER}

      BCRYPT_ROUNDS: ${BCRYPT_ROUNDS}

      LOG_CHANNEL: ${LOG_CHANNEL}
      LOG_STACK: ${LOG_STACK}
      LOG_DEPRECATIONS_CHANNEL: ${LOG_DEPRECATIONS_CHANNEL}
      LOG_LEVEL: ${LOG_LEVEL}

      DB_CONNECTION: ${DB_CONNECTION}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}

      SESSION_DRIVER: ${SESSION_DRIVER}
      SESSION_LIFETIME: ${SESSION_LIFETIME}
      SESSION_ENCRYPT: ${SESSION_ENCRYPT}
      SESSION_PATH: ${SESSION_PATH}
      SESSION_DOMAIN: ${SESSION_DOMAIN}
      SESSION_SECURE_COOKIE: ${SESSION_SECURE_COOKIE}
      SESSION_HTTP_ONLY: ${SESSION_HTTP_ONLY}
      SESSION_SAME_SITE: ${SESSION_SAME_SITE}

      CACHE_STORE: ${CACHE_STORE}
      CACHE_PREFIX: ${CACHE_PREFIX}

      QUEUE_CONNECTION: ${QUEUE_CONNECTION}

      BROADCAST_CONNECTION: ${BROADCAST_CONNECTION}

      FILESYSTEM_DISK: ${FILESYSTEM_DISK}

      SUPABASE_S3_ACCESS_KEY_ID: ${SUPABASE_S3_ACCESS_KEY_ID}
      SUPABASE_S3_SECRET_ACCESS_KEY: ${SUPABASE_S3_SECRET_ACCESS_KEY}
      SUPABASE_S3_REGION: ${SUPABASE_S3_REGION}
      SUPABASE_STORAGE_BUCKET: ${SUPABASE_STORAGE_BUCKET}
      SUPABASE_S3_ENDPOINT: ${SUPABASE_S3_ENDPOINT}

      AWS_USE_PATH_STYLE_ENDPOINT: ${AWS_USE_PATH_STYLE_ENDPOINT}

      VITE_APP_NAME: ${VITE_APP_NAME}

    depends_on:
      db:
        condition: service_healthy

    networks:
      - e-arsip-network

    volumes:
      - e-arsip-storage:/var/www/html/storage
      - e-arsip-build:/var/www/html/public/build

    expose:
      - "9000"


  # =======================================================
  # NGINX
  # =======================================================

  nginx:
    image: nginx:alpine

    container_name: e-arsip-nginx

    restart: unless-stopped

    depends_on:
      - app

    ports:
      - "127.0.0.1:8088:80"

    volumes:
      # Project Laravel
      - ./:/var/www/html

      # Persistent Laravel storage
      - e-arsip-storage:/var/www/html/storage

      # Persistent compiled frontend
      - e-arsip-build:/var/www/html/public/build

      # Nginx configuration directory
      - ./docker/nginx:/etc/nginx/conf.d:ro

    networks:
      - e-arsip-network


  # =======================================================
  # MYSQL
  # =======================================================

  db:
    image: mysql:8.4

    container_name: e-arsip-db

    restart: unless-stopped

    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}

    volumes:
      - e-arsip-mysql-data:/var/lib/mysql

    networks:
      - e-arsip-network

    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-u${DB_USERNAME}",
          "-p${DB_PASSWORD}"
        ]
      interval: 10s
      timeout: 5s
      retries: 10


# =========================================================
# NETWORK
# =========================================================

networks:

  e-arsip-network:
    driver: bridge


# =========================================================
# VOLUMES
# =========================================================

volumes:

  e-arsip-storage:
    name: e-arsip-storage

  e-arsip-build:
    name: e-arsip-build

  e-arsip-mysql-data:
    name: e-arsip-mysql-data